<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">



    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="lenghtsUnits"
        android:title="@string/pref_title_lenght_units"
        android:defaultValue="0"
        android:entries="@array/pref_lenght_units_list_titles"
        android:entryValues="@array/pref_lenght_units_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="tempritureUnits"
        android:title="@string/pref_title_tempriture_units"
        android:defaultValue="0"
        android:entries="@array/pref_tempriture_units_list_titles"
        android:entryValues="@array/pref_tempriture_units_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

</PreferenceScreen>
